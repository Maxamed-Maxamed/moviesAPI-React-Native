import { View, Text } from 'react-native'
import React from 'react'

const CustomButton = ({
    title,
    handlePress,
    containerStyles,
    textStyles,
    isLoading,

}) => {
  return (
    <View>
      <Text>CustomButton hello </Text>
    </View>
  )
}

export default CustomButton